# Blog Starter

A blog example with:
- Static generation (`generateStaticParams`)
- Dynamic routes (`[slug]`)
- Layout chains
- Metadata API (`generateMetadata`)
- Tailwind CSS

## Run

```bash
pnpm install
pnpm dev
```
