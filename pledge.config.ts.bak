import { defineConfig } from 'pledge';

export default defineConfig({
  entry: ['packages/cli/src/bin.ts'],
  framework: 'react',
  app_dir: 'app',
  source_maps: true,
  env_prefix: ['PLEDGE_'],
  env_dts: true,
  compress_gzip: true,
  compress_brotli: true,
  define: {
    'process.env.PLEDGE_VERSION': '"0.0.1"',
  },
  dev_server: {
    port: 3001,
    host: 'localhost',
    hmr: true,
    open: false,
  },
  watch: {
    enabled: false,
    debounce_ms: 300,
  },
});
