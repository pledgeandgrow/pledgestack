# @pledgepack/win32-x64-msvc

Native pledgepack binary for Windows x64 (MSVC build).

This package is automatically installed as an optional dependency of `pledgepack` on Windows x64 systems. You should not need to install it directly.
