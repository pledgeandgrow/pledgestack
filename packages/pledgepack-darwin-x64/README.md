# @pledgepack/darwin-x64

Native pledgepack binary for macOS x64 (Intel).

This package is automatically installed as an optional dependency of `pledgepack` on macOS x64 systems. You should not need to install it directly.
