# @pledgepack/linux-x64-gnu

Native pledgepack binary for Linux x64 (GNU).

This package is automatically installed as an optional dependency of `pledgepack` on Linux x64 systems. You should not need to install it directly.
