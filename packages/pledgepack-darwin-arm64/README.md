# @pledgepack/darwin-arm64

Native pledgepack binary for macOS arm64 (Apple Silicon).

This package is automatically installed as an optional dependency of `pledgepack` on macOS arm64 systems. You should not need to install it directly.
